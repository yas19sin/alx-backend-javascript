// Define RowID type as a number
export type RowID = number;

// Define RowElement interface with required fields
export interface RowElement {
    firstName: string;
    lastName: string;
    age?: number;
}